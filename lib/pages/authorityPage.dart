import 'package:flutter/material.dart';

class authorityPage extends StatelessWidget {
  const authorityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
